using CitySimLibrary.Models.NPCs;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace TjuvOchPolis
{
    public class DrawingNPCs
    {
        public void NPCPlacer(NPC person)
        {
            char one = 'P';
            int direction = 1;
           // int[] pos = ;

            //if (pos[0] > 129 || pos[1] > 29)
            //{
            //    pos[0] -= 128;
            //    pos[1] -= 29;
            //}
            //Console.SetCursorPosition(pos[0], pos[1]);
            //Console.Write(one);
            //pos[0] += direction;
            //pos[1] += direction;


        }
    }
}